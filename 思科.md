## 基本命令

| 功能     | 命令                 |
| -------- | -------------------- |
| 特权模式 | `enable`             |
| 配置终端 | `configure terminal` |
| 设备改名 | `hostname <设备名>`  |
| 路由功能 | `ip routing`         |
| 退出     | `exit`               |

## 接口

| 功能       | 命令                                                |
| ---------- | --------------------------------------------------- |
| 进入接口   | `interface <接口>`</br>`interface range <接口范围>` |
| 配置IP地址 | `ip address <IP地址> <子网掩码>`                    |
| 加入 VLAN  | `switchport access vlan <VLAN号>`                   |

### 端口

| 功能     | 命令            |
| -------- | --------------- |
| 交换端口 | `switchport`    |
| 路由端口 | `no switchport` |

### Trunk 接口

| 功能         | 命令                                   |
| ------------ | -------------------------------------- |
| 封装         | `switchport trunk encapsulation dot1q` |
| 配置         | `switchport mode trunk`                |
| 配置时钟频率 | `clock rate <频率>`                    |

## VLAN

| 功能 | 命令                      |
| ---- | ------------------------- |
| 创建 | `vlan <VLAN号>`           |
| 进入 | `interface vlan <VLAN号>` |
| 改名 | `name <VLAN名>`           |

## 路由协议

网段地址：IP地址和子网掩码与运算

### RIP

| 功能         | 命令                 |
| ------------ | -------------------- |
| 配置路由     | `route rip`          |
| 配置版本     | `version 2`          |
| 配置网络     | `network <网段地址>` |
| 关闭自动汇总 | `no auto-summary`    |

### OSPF

| 功能     | 命令                                        |
| -------- | ------------------------------------------- |
| 配置路由 | `route ospf <进程号>`                       |
| 配置网络 | `network <网段地址> <反掩码> area <区域号>` |

### 静态路由

    ip route <网段地址> <子网掩码> <下一跳地址/端口>

### 缺省/默认路由

    ip route 0.0.0.0  0.0.0.0 下一跳地址/端口

## PPP 协议

**两端封装：`encapsulation ppp`**

### PAP 认证

| 认证类型 | 命令                                                                                                                                                                              |
| -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 单向认证 | **认证端配置：**<br>`ppp authentication pap`<br>`username <对方用户名> password <对方密码>`<br><br>**被认证端配置：**<br>`ppp pap sent-username <对方用户名> password <对方密码>` |
| 双向认证 | **两端配置：**<br>`ppp authentication pap`<br>`username <对方用户名> password <对方密码>`<br>`ppp pap sent-username <对方用户名> password <对方密码>`                             |

### CHAP 认证

| 认证类型 | 命令                                                                                                               |
| -------- | ------------------------------------------------------------------------------------------------------------------ |
| 单向认证 | **认证端配置：**<br>`ppp authentication chap`<br><br>**两端配置：**<br>`username <对方用户名> password <对方密码>` |
| 双向认证 | **两端配置：**<br>`ppp authentication chap`<br>`username <对方用户名> password <对方密码>`                         |

## Telnet

| 功能             | 命令                        |
| ---------------- | --------------------------- |
| 开启虚拟终端接口 | `line vty 0 4(开启0-4接口)` |
| 配置登录明文     | `password <明文>`           |
| 启用登录验证     | `login`                     |
| 配置特权密码     | `enable secret <密码>`      |

